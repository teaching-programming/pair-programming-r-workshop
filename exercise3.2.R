# Exercise 3.2: 

library(purrr)

# start by copy-pasting your definition of function from above
categorize_value <- function....

values <- c(45, 60, 150, 85, 20)
map_chr(values, categorize_value )

